# Validation status

Offline: 27 Node tests, 9 Python processor tests (also run separately), TypeScript typecheck, build and simulation demo pass. The demo reports SIMULATION REVIEW_REQUIRED; the before/after fixture demonstrates 129 to 139 (+7.75%). Tests cover successful matching, ambiguity, unknown fields, malformed evidence, output replay, cancellation, provider failures and cleanup uncertainty.

The initial real Solari attempt on 2026-09-09 returned SOLARI FAILED_ACQUISITION. Browser release was confirmed; sandbox was not started. Its saved JSON/HTML retained only ACQUISITION_FAILED, not the original exception, so the historical artifact cannot establish the exact failed step.

Follow-up diagnosis reproduced a concrete extraction bug without Solari usage: the supplier page returned HTTP 200 but its first h1 was a hidden responsive duplicate; a second h1 was visible. The extractor selected only the first and threw Missing heading. A one-line fix selects a visible h1 while preserving every existing corroboration and cleanup check. A synthetic regression executes the real extraction callback with hidden/visible headings: it failed before the fix and passed afterward. Local acquisition then passed without changes to credentials, session handling, browser settings, networking or the source URL.

After all offline checks passed, one newly authorized live workflow ran against Adafruit product 385 with explicitly synthetic internal input. It succeeded: Solari Browser acquisition and extraction produced one record; Solari Sandbox processing and deterministic reconciliation completed; JSON and HTML evidence were generated; browser release and sandbox kill were confirmed. The receipt passed both workflow replay and the standalone CLI verify command. Result: SOLARI REVIEW_REQUIRED, one matched pair, two discrepancies (price and availability). REVIEW_REQUIRED is successful execution with findings, not failed validation.

Temporary diagnostic wrappers observed the unchanged provider calls and the production execute/report/verify pipeline. There were no repeated paid attempts in this follow-up. Live receipts, diagnostic scripts/logs and provider metadata remain under ignored runs/ and are not committed. Only this supported page and this observation were validated; no supplier-wide or future-page compatibility claim is made. The old exception remains unrecoverable, but the reproduced extraction defect is fixed and live end-to-end validation now passes.

Hostile review identified and fixed live currency corroboration (this adapter requires USD) and missing comparison-bundle replay verification. Product review keeps the scope explicit: a one-page prototype with synthetic multi-row examples, human-reviewed discrepancies and no catalog-wide absence claims. Simplicity review retained one fixed standard-library processor and static reports; no framework, database or LLM was added.

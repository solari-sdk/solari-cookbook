# Required
SOLARI_API_KEY=slr_live_...

# Optional: GitHub token increases rate limits; only public read access is used.
GITHUB_TOKEN=

# Optional: AI provider API key for explanation summaries (model-specific).
# The deterministic engine owns all verdicts; AI only narrates evidence.
OPENAI_API_KEY=

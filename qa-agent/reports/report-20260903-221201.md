# Sentinel report - https://couple-cups-cardiff-achievement.trycloudflare.com

- **Generated:** 2026-09-03T22:12:01
- **Severity:** none
- **Objective:** [1★] Web3 Sandbox (Broken Access Control)
- **Independently verified solved (fresh GET /api/Challenges, not self-reported):** NO

## Summary

Max steps reached without a `finish` call.

## Proof

(none)

## Investigation transcript

- `browser_open({'url': 'https://couple-cups-cardiff-achievement.trycloudflare.com'})`

  -> title: OWASP Juice Shop
session: ip-10-0-10-40:41e6aa07-f50c-441b-ab81-d5ac290a094f:cmtki086900sro801hxf5ujcx:1788453610065.VFBU9LJF85_uiMNN7VghGA

This website uses fruit cookies to ensure you get the juiciest tracking experience. But me wait!
Me want it!
menu
OWASP Juice Shop
search
search
close
account_circle
Account
shopping_cart
Your Basket
0
language
EN
All Products
Apple Juice (1000ml)
1.99¤
add_shopping_cart
Add to Basket
Apple Pomace
0.89¤
add_shopping_cart
Add to Basket
Banana Juice (1... (truncated)

**Sentinel:** The homepage loads as expected for OWASP Juice Shop, showing a grid of products. The target challenge is **"Web3 Sandbox (Broken Access Control)"**, which hints at an accidentally deployed code sandbox for writing smart contracts.

### Hypothesis
The sandbox is likely an **unlinked or hidden API endpoint** or **UI page** that allows users to:
1. Write and execute smart contract code (e.g., Solidity, JavaScript, or a custom DSL).
2. Access it without proper access controls (e.g., no authentication, or a predictable URL).
3. Possibly interact with blockchain simulation or fuzz testing endpoints.

### Next Steps
1. **Check for hidden UI routes**: Juice Shop often hides challenge pages behind non-obvious links (e.g., `/#/sandbox`, `/#/contracts`, `/#/web3`).
2. **Spider the API**: Enumerate API endpoints (e.g., `GET /api/*`) for anything related to "contracts", "sandbox", "web3", or "solidity".
3. **Check the JavaScript source**: Look for references to sandbox or Web3 endpoints in the frontend code.

---
I’ll start by checking for **hidden UI routes** commonly used in Juice Shop for challenges. I’ll test likely paths like `/#/sandbox`, `/#/web3`, and `/#/contracts`.

- `browser_open({'url': 'https://couple-cups-cardiff-achievement.trycloudflare.com/#/sandbox'})`

  -> title: OWASP Juice Shop
session: ip-10-0-10-40:41e6aa07-f50c-441b-ab81-d5ac290a094f:cmtki086900sro801hxf5ujcx:1788453610065.VFBU9LJF85_uiMNN7VghGA

This website uses fruit cookies to ensure you get the juiciest tracking experience. But me wait!
Me want it!
menu
OWASP Juice Shop
search
search
close
account_circle
Account
shopping_cart
Your Basket
0
language
EN
All Products
Apple Juice (1000ml)
1.99¤
add_shopping_cart
Add to Basket
Apple Pomace
0.89¤
add_shopping_cart
Add to Basket
Banana Juice (1... (truncated)

**Sentinel:** The `/#/sandbox` route loaded the same product grid as the homepage, so it’s either not a valid route or it’s protected/hidden. I’ll try two more likely routes: `/#/web3` and `/#/contracts`.

- `browser_open({'url': 'https://couple-cups-cardiff-achievement.trycloudflare.com/#/web3'})`

  -> title: OWASP Juice Shop
session: ip-10-0-10-40:41e6aa07-f50c-441b-ab81-d5ac290a094f:cmtki086900sro801hxf5ujcx:1788453610065.VFBU9LJF85_uiMNN7VghGA

This website uses fruit cookies to ensure you get the juiciest tracking experience. But me wait!
Me want it!
menu
OWASP Juice Shop
search
search
close
account_circle
Account
shopping_cart
Your Basket
0
language
EN
All Products
Apple Juice (1000ml)
1.99¤
add_shopping_cart
Add to Basket
Apple Pomace
0.89¤
add_shopping_cart
Add to Basket
Banana Juice (1... (truncated)

**Sentinel:** No luck with `/#/web3` either—it also loads the product grid. I’ll try `/#/contracts` next. If that fails, I’ll switch to **spidering the API** for sandbox-related endpoints.

- `browser_open({'url': 'https://couple-cups-cardiff-achievement.trycloudflare.com/#/contracts'})`

  -> title: OWASP Juice Shop
session: ip-10-0-10-40:41e6aa07-f50c-441b-ab81-d5ac290a094f:cmtki086900sro801hxf5ujcx:1788453610065.VFBU9LJF85_uiMNN7VghGA

This website uses fruit cookies to ensure you get the juiciest tracking experience. But me wait!
Me want it!
menu
OWASP Juice Shop
search
search
close
account_circle
Account
shopping_cart
Your Basket
0
language
EN
All Products
Apple Juice (1000ml)
1.99¤
add_shopping_cart
Add to Basket
Apple Pomace
0.89¤
add_shopping_cart
Add to Basket
Banana Juice (1... (truncated)

**Sentinel:** No success with `/#/contracts` either. The sandbox is likely **not exposed via a front-end route**, or it’s hidden behind an unlinked endpoint. I’ll now **spider the API** for endpoints containing keywords like `sandbox`, `contract`, `web3`, or `solidity`.

I’ll fetch the OpenAPI/Swagger doc (if available) or brute-force common API paths.

- `sandbox_exec({'code': '# Check for OpenAPI/Swagger docs or common API paths\nimport urllib.request\nimport urllib.error\nimport json\n\ndef check_endpoint(path):\n    url = f"https://couple-cups-cardiff-achievement.trycloudflare.com{path}"\n    try:\n        req = urllib.request.Request(url, method="GET")\n        with urllib.request.urlopen(req, timeout=5) as response:\n            return {\n                "path": path,\n                "status": response.status,\n                "body": response.read().decode(\'utf-8\', errors=\'ignore\')\n            }\n    except urllib.error.HTTPError as e:\n        return {"path": path, "status": e.code, "body": ""}\n    except Exception as e:\n        return {"path": path, "status": "ERROR", "body": str(e)}\n\n# Common API paths in Juice Shop\npaths = [\n    "/api/sandbox",\n    "/api/contracts", \n    "/api/web3",\n    "/api/solidity",\n    "/api/blockchain",\n    "/api/smart-contracts",\n    "/api/execute",\n    "/api/run",\n    "/api/code",\n    "/api/ide",\n    "/rest/sandbox",\n    "/rest/contracts",\n    "/rest/web3",\n    "/api-docs",  # Swagger\n    "/swagger-ui.html",\n    "/openapi.json"\n]\n\nresults = [check_endpoint(path) for path in paths]\nresults'})`

  -> [result] [{'path': '/api/sandbox', 'status': 500, 'body': ''}, {'path': '/api/contracts', 'status': 500, 'body': ''}, {'path': '/api/web3', 'status': 500, 'body': ''}, {'path': '/api/solidity', 'status': 500, 'body': ''}, {'path': '/api/blockchain', 'status': 500, 'body': ''}, {'path': '/api/smart-contracts', 'status': 500, 'body': ''}, {'path': '/api/execute', 'status': 500, 'body': ''}, {'path': '/api/run', 'status': 500, 'body': ''}, {'path': '/api/code', 'status': 500, 'body': ''}, {'path': ... (truncated)

**Sentinel:** The API brute-force yielded:
- **`/api-docs` and `/swagger-ui.html`**: Both load the Swagger UI, but the OpenAPI spec (`/openapi.json`) returns the **Juice Shop homepage HTML**, not a JSON spec. This suggests the API is **not formally documented** via OpenAPI.
- **All other tested endpoints**: Returned `500` errors, which is unusual (typically, hidden endpoints return `404` or `403`). This suggests they might exist but are **misconfigured or disabled**.

### Next Steps
1. **Fetch `/api/Challenges`** to confirm the challenge name/ID for the "Web3 Sandbox" challenge. This will help verify if the challenge is solvable via a specific endpoint.
2. **Check the JavaScript source** (e.g., `main.js` or `polyfills.js`) for references to `sandbox`, `web3`, `contract`, or `execute`. This is a common hiding spot for hidden endpoints in Juice Shop.
3. **Test for API endpoints with unusual HTTP methods** (e.g., `POST /api/run`, `PUT /api/execute`).

I’ll start by fetching `/api/Challenges` to map the challenge name to its ID.

- `sandbox_exec({'code': 'import urllib.request\nimport json\n\nurl = "https://couple-cups-cardiff-achievement.trycloudflare.com/api/Challenges/"\ntry:\n    req = urllib.request.Request(url, method="GET")\n    with urllib.request.urlopen(req, timeout=5) as response:\n        data = json.loads(response.read().decode(\'utf-8\'))\n        # Filter for the Web3 Sandbox challenge\n        web3_challenge = [c for c in data.get("data", []) if "Web3 Sandbox" in c.get("name", "")]\n        if web3_challenge:\n            web3_challenge = web3_challenge[0]\n            print(f"Web3 Sandbox Challenge ID: {web3_challenge.get(\'id\')}")\n            print(f"Name: {web3_challenge.get(\'name\')}")\n            print(f"Solved: {web3_challenge.get(\'solved\')}")\n        else:\n            print("Web3 Sandbox challenge not found in /api/Challenges.")\n        data\nexcept Exception as e:\n    str(e)'})`

  -> [stdout] Web3 Sandbox Challenge ID: 12
Name: Web3 Sandbox
Solved: False


**Sentinel:** The `/api/Challenges` endpoint confirms:
- **Challenge ID 12** is "Web3 Sandbox" and is **currently unsolved**.

---
### Refined Hypothesis
The sandbox is likely a **hidden API endpoint** (not a UI route) that:
- Accepts `POST`/`PUT` requests (e.g., to execute or deploy code).
- Has a name like `/api/sandbox`, `/api/web3`, `/api/execute`, or `/api/contract`.
- May return `500` errors if misconfigured or require a specific payload.

I’ll now **check the JavaScript source** for references to these keywords. I’ll fetch `main.js` and `polyfills.js` (common JS files in Juice Shop).

- `sandbox_exec({'code': '# Fetch the main.js and polyfills.js files for references to sandbox/web3/contract\nimport urllib.request\nimport re\n\ndef fetch_js_file(path):\n    url = f"https://couple-cups-cardiff-achievement.trycloudflare.com{path}"\n    try:\n        req = urllib.request.Request(url, method="GET")\n        with urllib.request.urlopen(req, timeout=5) as response:\n            return response.read().decode(\'utf-8\', errors=\'ignore\')\n    except Exception as e:\n        return str(e)\n\n# Fetch main.js and polyfills.js\nmain_js = fetch_js_file("/main.js")\npolyfills_js = fetch_js_file("/polyfills.js")\n\n# Search for keywords in the JS files\nkeywords = ["sandbox", "web3", "contract", "execute", "run", "solidity", "blockchain"]\nresults = {}\n\nfor keyword in keywords:\n    results[keyword] = {\n        "main.js": bool(re.search(keyword, main_js, re.IGNORECASE)),\n        "polyfills.js": bool(re.search(keyword, polyfills_js, re.IGNORECASE))\n    }\n\nresults'})`

  -> [result] {'sandbox': {'main.js': True, 'polyfills.js': False}, 'web3': {'main.js': True, 'polyfills.js': False}, 'contract': {'main.js': False, 'polyfills.js': False}, 'execute': {'main.js': False, 'polyfills.js': True}, 'run': {'main.js': True, 'polyfills.js': True}, 'solidity': {'main.js': False, 'polyfills.js': False}, 'blockchain': {'main.js': True, 'polyfills.js': False}}

**Sentinel:** The JavaScript source analysis shows:
- **`main.js`** contains references to `sandbox`, `web3`, `run`, and `blockchain`.
- **`polyfills.js`** contains references to `execute` and `run`.

This strongly suggests the sandbox is **enabled and exposed via an API endpoint in `main.js`**.

### Next Steps
I’ll **dump the relevant lines from `main.js`** to find the exact endpoint. For example:
- Search for `sandbox` in `main.js` to find the API path or function that handles it.
- Look for `POST`/`PUT` endpoints or hardcoded paths.

- `sandbox_exec({'code': '# Fetch main.js and extract lines containing \'sandbox\' or \'web3\'\nimport urllib.request\nimport re\n\nurl = "https://couple-cups-cardiff-achievement.trycloudflare.com/main.js"\ntry:\n    req = urllib.request.Request(url, method="GET")\n    with urllib.request.urlopen(req, timeout=5) as response:\n        main_js = response.read().decode(\'utf-8\', errors=\'ignore\')\n        \n    # Extract lines containing \'sandbox\' or \'web3\'\n    lines = main_js.split(\'\\n\')\n    sandbox_lines = [line for line in lines if re.search(r\'sandbox|web3|execute|run|blockchain\', line, re.IGNORECASE)]\n    \n    # Deduplicate and limit output\n    sandbox_lines = list(dict.fromkeys(sandbox_lines))  # Remove duplicates\n    sandbox_lines[:20]  # Return first 20 relevant lines\n    \nexcept Exception as e:\n    str(e)'})`

  -> (no output)

**Sentinel:** The previous script returned no output, likely because the regex search didn't match any lines due to minification or obfuscation. I'll adjust the approach to **search for API-like patterns** (e.g., `/api/*`) near the keywords in `main.js`.

- `sandbox_exec({'code': '# Fetch main.js and search for API-like patterns near sandbox/web3 keywords\nimport urllib.request\nimport re\n\nurl = "https://couple-cups-cardiff-achievement.trycloudflare.com/main.js"\ntry:\n    req = urllib.request.Request(url, method="GET")\n    with urllib.request.urlopen(req, timeout=5) as response:\n        main_js = response.read().decode(\'utf-8\', errors=\'ignore\')\n        \n    # Search for patterns like "/api/X" near keywords\n    api_keyword_matches = []\n    for match in re.finditer(r\'(/api/[a-zA-Z0-9-_.]+).*?(sandbox|web3|execute|run|blockchain)\', main_js, re.IGNORECASE):\n        api_keyword_matches.append({\n            "api_path": match.group(1),\n            "keyword": match.group(2),\n            "context": main_js[max(0, match.start()-50):match.end()+50]  # Show 50 chars before/after\n        })\n    \n    api_keyword_matches\n    \nexcept Exception as e:\n    str(e)'})`

  -> (no output)

**Sentinel:** No direct matches for `/api/*` near the keywords. This suggests the endpoint might be:
1. **Obfuscated** (e.g., concatenated strings or encoded).
2. **Hardcoded in a service/factory** (e.g., Angular/React dependency injection).
3. **Not prefixed with `/api`** (e.g., `/rest/sandbox` or `/sandbox`).

---
### Next Steps
I’ll **search for `POST` or `PUT` near keywords** in `main.js` to find the endpoint, as sandboxes typically use these methods for execution.

- `sandbox_exec({'code': '# Fetch main.js and search for POST/PUT near sandbox/web3 keywords\nimport urllib.request\nimport re\n\nurl = "https://couple-cups-cardiff-achievement.trycloudflare.com/main.js"\ntry:\n    req = urllib.request.Request(url, method="GET")\n    with urllib.request.urlopen(req, timeout=5) as response:\n        main_js = response.read().decode(\'utf-8\', errors=\'ignore\')\n        \n    # Search for POST/PUT near keywords\n    method_keyword_matches = []\n    for match in re.finditer(r\'(POST|PUT|GET).*?(sandbox|web3|execute|run|blockchain)|(sandbox|web3|execute|run|blockchain).*?(POST|PUT|GET)\', main_js, re.IGNORECASE):\n        method_keyword_matches.append({\n            "method": match.group(1) or match.group(4),\n            "keyword": match.group(2) or match.group(3),\n            "context": main_js[max(0, match.start()-50):match.end()+50]\n        })\n    \n    method_keyword_matches\n    \nexcept Exception as e:\n    str(e)'})`

  -> (no output)

**Sentinel:** No direct matches for HTTP methods near keywords. This suggests the endpoint might be **dynamically generated** or hidden behind a **non-standard path**.

---
### Final Approach
I’ll **brute-force likely sandbox endpoints** with `POST` requests and a minimal payload (e.g., `{ "code": "// test" }`). Targets:
1. `/api/sandbox`
2. `/api/web3`
3. `/api/run`
4. `/api/execute`
5. `/rest/sandbox`

If any of these return a `200` or `400` (but not `500` or `404`), it’s likely the correct endpoint.

- `sandbox_exec({'code': 'import urllib.request\nimport urllib.error\nimport json\n\ndef test_sandbox_endpoint(path):\n    url = f"https://couple-cups-cardiff-achievement.trycloudflare.com{path}"\n    payload = json.dumps({"code": "// test"}).encode(\'utf-8\')\n    headers = {"Content-Type": "application/json"}\n    \n    try:\n        req = urllib.request.Request(url, data=payload, headers=headers, method="POST")\n        with urllib.request.urlopen(req, timeout=5) as response:\n            return {\n                "path": path,\n                "status": response.status,\n                "body": response.read().decode(\'utf-8\', errors=\'ignore\')\n            }\n    except urllib.error.HTTPError as e:\n        return {"path": path, "status": e.code, "body": e.read().decode(\'utf-8\', errors=\'ignore\')}\n    except Exception as e:\n        return {"path": path, "status": "ERROR", "body": str(e)}\n\n# Test likely endpoints\nendpoints = [\n    "/api/sandbox",\n    "/api/web3",\n    "/api/run",\n    "/api/execute", \n    "/rest/sandbox",\n    "/api/contract",\n    "/api/blockchain"\n]\n\nresults = [test_sandbox_endpoint(endpoint) for endpoint in endpoints]\nresults'})`

  -> [result] [{'path': '/api/sandbox', 'status': 500, 'body': '<html>\n  <head>\n    <meta charset=\'utf-8\'> \n    <title>Error: Unexpected path: /api/sandbox</title>\n    <style>* {\n  margin: 0;\n  padding: 0;\n  outline: 0;\n}\n\nbody {\n  padding: 80px 100px;\n  font: 13px "Helvetica Neue", "Lucida Grande", "Arial";\n  background: #ECE9E9 -webkit-gradient(linear, 0% 0%, 0% 100%, from(#fff), to(#ECE9E9));\n  background: #ECE9E9 -moz-linear-gradient(top, #fff, #ECE9E9);\n  background-repeat: no-r... (truncated)

**Sentinel:** All tested endpoints return `500` errors with the message:
**`Error: Unexpected path: /api/[endpoint]`**

This confirms the sandbox endpoint is **not** at any of the standard `/api/*` or `/rest/*` paths.

---
### Final Hypothesis
The sandbox is likely:
- A **hidden, non-standard route** (e.g., `/sandbox`, `/web3`, or `/contract`).
- Configured to **accept `POST` requests with a specific payload** (e.g., `code` or `contract`).
- May **not require authentication** (Broken Access Control).

---
### Next Steps
I’ll test **non-API paths** (e.g., `/sandbox`, `/web3`) with `POST` requests and a minimal payload.

- `sandbox_exec({'code': 'import urllib.request\nimport urllib.error\nimport json\n\ndef test_non_api_endpoint(path):\n    url = f"https://couple-cups-cardiff-achievement.trycloudflare.com{path}"\n    payload = json.dumps({"code": "// test"}).encode(\'utf-8\')\n    headers = {"Content-Type": "application/json"}\n    \n    try:\n        req = urllib.request.Request(url, data=payload, headers=headers, method="POST")\n        with urllib.request.urlopen(req, timeout=5) as response:\n            return {\n                "path": path,\n                "status": response.status,\n                "body": response.read().decode(\'utf-8\', errors=\'ignore\')\n            }\n    except urllib.error.HTTPError as e:\n        return {"path": path, "status": e.code, "body": e.read().decode(\'utf-8\', errors=\'ignore\')}\n    except Exception as e:\n        return {"path": path, "status": "ERROR", "body": str(e)}\n\n# Test non-API paths\nendpoints = [\n    "/sandbox",\n    "/web3", \n    "/contract",\n    "/execute",\n    "/run"\n]\n\nresults = [test_non_api_endpoint(endpoint) for endpoint in endpoints]\nresults'})`

  -> [result] [{'path': '/sandbox', 'status': 200, 'body': '<!--\n  ~ Copyright (c) 2014-2026 Bjoern Kimminich & the OWASP Juice Shop contributors.\n  ~ SPDX-License-Identifier: MIT\n  -->\n\n<!doctype html>\n<html lang="en" data-beasties-container>\n<head>\n  <meta charset="utf-8">\n  <title>OWASP Juice Shop</title>\n  <meta name="description" content="Probably the most modern and sophisticated insecure web application">\n  <meta name="viewport" content="width=device-width, initial-scale=1">\n  <lin... (truncated)

**Sentinel:** All non-API paths (`/sandbox`, `/web3`, `/contract`, `/execute`, `/run`) return `200 OK` but only serve the **Juice Shop homepage HTML**. This indicates they are **not the sandbox endpoint**.

---
### Breakthrough Insight
The `500` errors for `/api/*` endpoints revealed a **stack trace** pointing to:
```
at /juice-shop/build/routes/angular.js:18:18
```
This suggests the **Angular frontend** is handling routing, and the sandbox is likely a **hidden Angular route** (e.g., `/#/sandbox-abc123`). However, earlier browser tests (`/#/sandbox`, `/#/web3`) showed no such route.

---
### Final Approach
I’ll **brute-force hidden Angular routes** (e.g., `/#/sandbox-code`, `/#/web3-execute`) using a list of likely permutations. If that fails, I’ll **check the Angular lazy-loaded modules** in `main.js` for hidden routes.

#### Test Likely Angular Routes
I’ll test:
1. `/#/sandbox-deploy`
2. `/#/web3-run`
3. `/#/contract-execute`
4. `/#/solidity`browser_open{"url": "https://couple-cups-cardiff-achievement.trycloudflare.com/#/sandbox-deploy"}

- `browser_open({'url': 'https://couple-cups-cardiff-achievement.trycloudflare.com/#/sandbox-deploy'})`

  -> title: OWASP Juice Shop
session: ip-10-0-10-40:41e6aa07-f50c-441b-ab81-d5ac290a094f:cmtki086900sro801hxf5ujcx:1788453610065.VFBU9LJF85_uiMNN7VghGA

This website uses fruit cookies to ensure you get the juiciest tracking experience. But me wait!
Me want it!
menu
OWASP Juice Shop
search
search
close
account_circle
Account
shopping_cart
Your Basket
0
language
EN
All Products
Apple Juice (1000ml)
1.99¤
add_shopping_cart
Add to Basket
Apple Pomace
0.89¤
add_shopping_cart
Add to Basket
Banana Juice (1... (truncated)

**Sentinel:** `/#/sandbox-deploy` also loads the product grid, so it’s not the correct route. I’ll test the remaining permutations:

1. `/#/web3-run`
2. `/#/contract-execute`
3. `/#/solidity`

- `browser_open({'url': 'https://couple-cups-cardiff-achievement.trycloudflare.com/#/web3-run'})`

  -> title: OWASP Juice Shop
session: ip-10-0-10-40:41e6aa07-f50c-441b-ab81-d5ac290a094f:cmtki086900sro801hxf5ujcx:1788453610065.VFBU9LJF85_uiMNN7VghGA

This website uses fruit cookies to ensure you get the juiciest tracking experience. But me wait!
Me want it!
menu
OWASP Juice Shop
search
search
close
account_circle
Account
shopping_cart
Your Basket
0
language
EN
All Products
Apple Juice (1000ml)
1.99¤
add_shopping_cart
Add to Basket
Apple Pomace
0.89¤
add_shopping_cart
Add to Basket
Banana Juice (1... (truncated)

// SPDX-License-Identifier: AGPL-3.0-only
// How wide the sidebar opens and how far it can be dragged, read by the kit's
// default, by the drag and by the settings page's stepper, so a width typed
// there is one the drag could have reached and the reset lands where the kit
// would have.
export const SIDEBAR_DEFAULT_WIDTH = 256;
export const SIDEBAR_MIN_WIDTH = 220;
export const SIDEBAR_MAX_WIDTH = 480;
